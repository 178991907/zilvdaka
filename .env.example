# Neon Database Connection String
# 1. Go to your Neon project dashboard.
# 2. Find the "Connection Details" card.
# 3. Copy the "Postgres URL" (it starts with postgresql://).
# 4. Paste it here.
# 5. VERY IMPORTANT: Rename this file from `.env.example` to just `.env`
DATABASE_URL="postgresql://user:password@host:port/dbname?sslmode=require"
